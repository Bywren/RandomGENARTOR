const emojis = ["🤡", "😹", "💔", "😿"];
const numbers = [1,2,3,4,5]

function setup() {
  createCanvas(800, 800);
}
{
function draw() {
  background(220);
  frameRate(1)
  for(number of numbers){}
  let x = random(0, 700);
  let y = random(0, 700);
  textSize(75)
  const emoji = random(emojis)
  text(emoji,x,y)
}

 
 
  }